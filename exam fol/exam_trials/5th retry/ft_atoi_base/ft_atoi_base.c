int	ft_atoi_base(const char *str, int str_base)
{
	int i = 0;
	char *s = (char*)str;

	//normal atoi
	//make helper function for checking if its upper case and lower case b16
	//loop through to convert to number
	// // if number, normal atoi conversion
	// // if a - f (lower or upper) atoi conversion but 'a' + 10 (upper case too)
	//
	// normal atoi return
	
}
